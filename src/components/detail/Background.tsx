import React from "react";

type Props = {};

const Background = (props: Props) => {
  return (
    <div className="absolute inset-0 bg-gradient-to-tl from-lime-200 to-teal-200" />
  );
};

export default Background;
